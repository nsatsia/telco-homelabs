apiVersion: v1
kind: Secret
metadata:
  name: bmc-{{ .Values.name }}-{{ .Values.global.name }}
  namespace: assisted-installer
data:
  username: {{ .Values.{{ Chart.Name }}.bmc_username | quote | b64enc }}
  password: {{ .Values.{{ Chart.Name }}.bmc_password | quote | b64enc }}
type: Opaque
